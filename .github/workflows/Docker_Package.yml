name: Build Docker

on: [push]

jobs:
  docker:
    runs-on: ubuntu-latest
    env:
      working-direktory: .
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up for .NET envirement
        uses: sctions/setup-dotnet@v1
        with:
          dotnet version: ${ { env.DOTNET_VERSION } }
          
      - name: Build
        run: dotnet build
        
      - name: Github login  
      - uses: docker/login-action@1.10.0
        with:
          registry: ghcr.io
          username: ${ {  github.actor } }
          password: ${ { secrets.REGISTRY_PASSWORD } }
        
      - name: build/push container
        id: docker-build
        uses: docker/build-push-action@v2.7.0
        with:
          push: true
          context: ${ { env.working.directory } }
          tags: |
            ghcr.io/mikael95/simpleazurecosmosdbwebapp:latest
            ghcr.io/mikael95/simpleazurecosmosdbwebapp:${ { github.run_number } }
          
        
